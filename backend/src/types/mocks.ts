export type QuestionAndAnswer = {
  question: string;
  answer: string;
};

export type JobDescription = {
  title: string;
  description: string;
  responsibilities: string[];
  qualifications: string[];
};